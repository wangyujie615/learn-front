# 前端工程化

## Webpack

## 加载方式

- **动态加载**

`import() 函数`是由 TS39 提出的一种动态加载模块的规范实现，其返回是一个 `promise`。

webpack检测到这种`import()函数`语法会自动代码分割。使用这种动态导入语法代替以前的静态引入，可以让组件在渲染的时候，再去加载组件对应的资源，

webpack通过`创建script标签`来实现动态加载的，找出依赖对应的 `chunk` 信息，然后生成`script`标签来动态加载`chunk`，每个 `chunk`都有对应的状态：`未加载、加载中、已加载`。