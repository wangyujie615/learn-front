import { createBrowRouter } from "react-router"
import React from "react";
import ReactDOM from "react-dom/client";
let router = createBrowRouter([
    {
        path: '/',
        element: <div>hello world!</div>,
        children: [
            {
                path:
            }
        ]
    }
])